# Minimal deep-baseline extras used for Phase E (T40-T45).
# We intentionally do not install rPPG-Toolbox's old pinned requirements as-is,
# because those pins conflict with the current Python 3.12 / Torch 2.11 setup.
yacs==0.1.8
tensorboardX==2.6.5
mat73==0.65
timm==1.0.27
thop==0.1.1.post2209072238
scikit-image==0.26.0
neurokit2==0.2.13
